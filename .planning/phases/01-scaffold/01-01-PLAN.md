---
phase: 01-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - tsconfig.json
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - tailwind.config.ts
autonomous: true
requirements:
  - FOUND-01
  - FOUND-05

must_haves:
  truths:
    - "`next build` completes with zero errors"
    - "A `/out` directory is produced containing static HTML"
    - "The project uses Next.js 15 App Router with TypeScript"
    - "Tailwind CSS v4 is installed and wired to the project"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with Next.js 15 and Tailwind CSS v4 deps"
      contains: "\"next\""
    - path: "next.config.ts"
      provides: "Static export configuration"
      contains: "output: 'export'"
    - path: "app/layout.tsx"
      provides: "Root layout with App Router conventions"
    - path: "app/page.tsx"
      provides: "Placeholder home page so the build has a route"
    - path: "app/globals.css"
      provides: "Tailwind v4 entry with @import"
  key_links:
    - from: "next.config.ts"
      to: "/out directory"
      via: "output: 'export' config flag"
      pattern: "output.*export"
    - from: "app/globals.css"
      to: "tailwind"
      via: "@import 'tailwindcss'"
      pattern: "@import.*tailwindcss"
---

<objective>
Initialize the Next.js 15 project with App Router, TypeScript, and Tailwind CSS v4. Configure `output: 'export'` for static site generation and verify `next build` completes cleanly with a `/out` directory produced.

Purpose: Every subsequent phase builds on this verified static-export foundation. If the build is broken, nothing downstream ships.
Output: A runnable Next.js 15 project that produces a clean static export.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 15 project with App Router and Tailwind CSS v4</name>
  <files>
    package.json, next.config.ts, tsconfig.json, app/layout.tsx, app/page.tsx,
    app/globals.css, tailwind.config.ts, postcss.config.mjs, .gitignore
  </files>

  <read_first>
    - CLAUDE.md (critical rules: no animation libraries, static export required)
    - .planning/phases/01-scaffold/01-CONTEXT.md (D-01 through D-03 brand token decisions)
  </read_first>

  <action>
Run the following command from `D:\VS Projects\Sask Garage Glow-up` to scaffold the project:

```
npx create-next-app@latest . --typescript --tailwind --app --src-dir=no --import-alias="@/*" --use-npm
```

If `create-next-app` prompts interactively, answer:
- TypeScript: Yes
- ESLint: Yes
- Tailwind CSS: Yes
- `src/` directory: No
- App Router: Yes
- Import alias: @/* (default)

After scaffolding, open `next.config.ts` and add `output: 'export'` to the config object:

```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: 'export',
};

export default nextConfig;
```

Verify `app/globals.css` contains the Tailwind v4 import. For Tailwind v4, the canonical import is:
```css
@import "tailwindcss";
```

If `create-next-app` installed Tailwind v3 (check package.json for `"tailwindcss"` version), upgrade to v4:
```
npm install tailwindcss@latest @tailwindcss/postcss@latest
```
Then update `postcss.config.mjs` to use the v4 plugin:
```js
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
export default config;
```
And update `app/globals.css` to use `@import "tailwindcss"` instead of the v3 `@tailwind` directives.

Do NOT install Framer Motion, any animation library, or any backend dependencies.
  </action>

  <verify>
    <automated>cd "D:\VS Projects\Sask Garage Glow-up" && npm run build 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `package.json` contains `"next"` at version `"15` (starts with 15)
    - `package.json` contains `"tailwindcss"` at version `"4` (starts with 4)
    - `next.config.ts` contains the string `output: 'export'`
    - `app/globals.css` contains `@import "tailwindcss"` (NOT `@tailwind base`)
    - `npm run build` exits with code 0
    - Directory `/out` exists after build with at least `index.html` inside it
  </acceptance_criteria>

  <done>
    Next.js 15 App Router project initialized with Tailwind CSS v4, `output: 'export'` configured, `npm run build` exits 0, and `/out/index.html` exists.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify static export output is valid</name>
  <files>
    out/index.html
  </files>

  <read_first>
    - next.config.ts (confirm output: 'export' is present)
    - out/index.html (inspect that it contains valid HTML)
  </read_first>

  <action>
After `npm run build` succeeds, inspect the output to confirm:
1. `out/index.html` exists
2. It contains a `<!DOCTYPE html>` declaration
3. It contains a `<html` tag

Run the following check commands:
```bash
# Confirm out directory was created
ls out/

# Confirm index.html exists and starts with doctype
head -5 out/index.html
```

If `out/index.html` is missing, the `output: 'export'` config is not being picked up. Re-read `next.config.ts` and confirm the config object uses `output: 'export'` (not `output: "export"` with a different syntax error).

No code changes are needed in this task — it is a verification step only. If the build is failing, return to Task 1 and fix the root cause before marking this task complete.
  </action>

  <verify>
    <automated>cd "D:\VS Projects\Sask Garage Glow-up" && head -3 out/index.html</automated>
  </verify>

  <acceptance_criteria>
    - File `out/index.html` exists on disk
    - First 3 lines of `out/index.html` contain `<!DOCTYPE html>`
    - `out/` directory contains at least one `.html` file
  </acceptance_criteria>

  <done>
    `out/index.html` exists, contains valid HTML, and confirms the static export pipeline is operational.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build toolchain | npm scripts run in the developer's local environment — no external trust boundary at this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | package.json | accept | Dev-only environment; lockfile (package-lock.json) pins transitive deps — low risk at scaffold phase |
| T-01-02 | Information Disclosure | next.config.ts | accept | No secrets at scaffold phase; `output: 'export'` is a public config value |
</threat_model>

<verification>
Run from project root:
```bash
npm run build && ls out/index.html && echo "BUILD OK"
```
Expected: `out/index.html` path listed and `BUILD OK` printed.
</verification>

<success_criteria>
- `npm run build` exits 0
- `out/index.html` exists and contains `<!DOCTYPE html>`
- `package.json` shows `next@15.x` and `tailwindcss@4.x`
- `next.config.ts` contains `output: 'export'`
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold/01-01-SUMMARY.md` using the summary template.
</output>
